dist: trusty

language: java

jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.m2
      - ./frontend/node_modules/

before_script:
  - npm install --prefix frontend/

script:
  - mvn clean test
  - npm run build --prefix frontend/
    - sonar-scanner
addons:
  sonarcloud:
    organization: "kor1k-github"
    token: 04ef28fbe0e838e7d492e2a63707dbf999ef7507
