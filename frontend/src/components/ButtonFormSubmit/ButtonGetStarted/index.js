import React from 'react'
import './ButtonGetStarted.scss'

export const ButtonGetStarted = props => {
    return (
        <button className='form-submit' type = {props.type}>GET STARTED</button>
    )
}